# Calorimeter Digitization Configuration - Single Pions with Log-Uniform Energy
campaign: "single_particle_pilot"
dataset: "single_pion_loguniform"
version: "v2"
stage: "calo_digitization"

job_config:
  n_runs: 1
  runs_per_node: 1
  time_limit: "00:30:00"
  qos: "debug"
  execution_mode: "interactive"

events: 1000
seed: 42

# Realistic digitization settings
apply_realistic_ecal_digi: 1  # 0=none, 1=silicon, 2=scintillator
apply_realistic_hcal_digi: 1  # 0=none, 1=scintillator

debug_output_dir: "/global/cfs/cdirs/m4958/data/ColliderML/simulation/single_particle_pilot/single_pion_loguniform/v1/runs/0"