campaign: "hard_scatter"
dataset: "susy_rpv"
version: "v1"
stage: "pythia_generation"  # One of: pythia_generation, madgraph_generation, simulation, digitization

job_config:
  n_runs: 1              # Number of runs (where a run produces N events)
  runs_per_node: 1         # Number of runs per node
  time_limit: "01:00:00"   # Time limit per job
  qos: "regular"
  # execution_mode: "multi_node_slurm"
  execution_mode: "interactive"

validation_config:

# Stage-specific configurations
events: 1          # Events per run
pileup: 0           # No pileup in hard-scatter SUSY sample

# Neutralino pair production; detailed spectrum and decays come from SLHA file
hard_process: "SUSY:qqbar2chi0chi0"

# Pythia settings list - all settings in Pythia8 format
pythia_settings:
  # Debug / output control
  - "Init:showProcesses = false"
  - "Init:showChangedSettings = false"
  - "Init:showAllSettings = false"
  - "Next:numberShowInfo = 0"
  - "Next:numberShowProcess = 0"
  - "Next:numberShowEvent = 0"
  - "Print:quiet = on"

  # SUSY spectrum and RPV LLP behaviour from SLHA
  - "SLHA:file = /global/cfs/cdirs/m4958/usr/danieltm/ColliderML/software/colliderml_dev/configs_production/hard_scatter/susy_rpv/rpv_llp.spc"
  - "SLHA:verbose = 3"
  - "SLHA:minMassSM = 100."
  # Enable decay table usage to respect RPV settings or overrides
  - "SLHA:useDecayTable = on"

  # RPV Decay Configuration for Neutralino (LSP) -> 3 quarks (UDD)
  # Force decay of neutralino (1000022) to d s b (ids 1 3 5)
  # meMode 0 = Isotropic decay
  - "1000022:oneChannel = 1 1.0 0 1 3 5"
  # Set lifetime c*tau in mm (approx 10 mm = 1 cm) for Displaced Vertex signature
  - "1000022:tau0 = 10.0"

  # Parton-level and hadronisation settings
  - "PartonLevel:MPI = on"
  - "PartonLevel:ISR = on"
  - "PartonLevel:FSR = on"
  - "HadronLevel:Hadronize = on"

  # Generic tune / PDFs
  - "Tune:ee = 7"
  - "PDF:pSet = 13"


debug_output_dir: "/global/cfs/cdirs/m4958/data/ColliderML/simulation/hard_scatter/susy_rpv/v1/runs/0"
