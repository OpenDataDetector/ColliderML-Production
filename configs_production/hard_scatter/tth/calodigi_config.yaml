# Calorimeter Digitization Configuration - ttH with Hard Scatter
campaign: "hard_scatter"
dataset: "tth"
version: "v1"
stage: "calo_digitization"

job_config:
  n_runs: 100
  runs_per_node: 64
  time_limit: "02:00:00"
  qos: "regular"
  # execution_mode: "interactive"
  execution_mode: "multi_node_slurm"

events: 1280
seed: 42

# Realistic digitization settings
apply_realistic_ecal_digi: 0  # 0=none, 1=silicon, 2=scintillator
apply_realistic_hcal_digi: 0  # 0=none, 1=scintillator

# debug_output_dir: "/global/cfs/cdirs/m4958/data/ColliderML/simulation/hard_scatter/tth/v1/runs/0"
