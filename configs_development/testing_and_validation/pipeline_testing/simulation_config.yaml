# Example configuration
dataset: "gg2ttbar"
version: "v1"
stage: "simulation"  # One of: generation, merge_smear, simulation, digitization
job_config:
  # Number of nodes will be calculated as n_runs / runs_per_node
  n_runs: 128  # Number of jobs to submit
  runs_per_node: 32  # Number of runs per node
  time_limit: "04:00:00"  # Time limit per job
  qos: "regular"

validation_config:
  time_limit: "00:10:00"
  qos: "regular"

# Stage-specific configurations
events: 10  # Number of events per run, total number of events will be n_runs * events

# Common settings
common:
  output_base_dir: "/global/cfs/projectdirs/m4958/data/ColliderML/outputs/madgraph_merge_testing/run_0"
  account: "m4958"
  container: "registry.cern.ch/atlasadc/atlas-grid-almalinux9"

output: "/global/cfs/projectdirs/m4958/data/ColliderML/outputs/madgraph_merge_testing/run_0"